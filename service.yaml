apiVersion: v1
kind: Service
metadata:
  labels:
    app: nl-blog
  name: nl-blog
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nl-blog
  type: ClusterIP
status:
  loadBalancer: {}
